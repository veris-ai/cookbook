name: Build Template Contexts

on:
  workflow_dispatch:

env:
  GCS_PREFIX: templates

jobs:
  build-and-upload:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write  # For Workload Identity Federation

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install PyYAML
        run: pip install pyyaml

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          project_id: ${{ vars.GCP_PROJECT_ID }}
          workload_identity_provider: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ vars.GCP_SERVICE_ACCOUNT }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Build contexts and manifest
        run: python .github/scripts/build_contexts.py

      - name: Upload to GCS (dev)
        run: |
          gsutil -m cp -r dist/contexts/* gs://veris-sandbox-blob-storage-dev/${{ env.GCS_PREFIX }}/contexts/
          gsutil cp dist/manifest.json gs://veris-sandbox-blob-storage-dev/${{ env.GCS_PREFIX }}/manifest.json

      - name: Upload to GCS (prod)
        run: |
          gsutil -m cp -r dist/contexts/* gs://veris-sandbox-blob-storage-prod/${{ env.GCS_PREFIX }}/contexts/
          gsutil cp dist/manifest.json gs://veris-sandbox-blob-storage-prod/${{ env.GCS_PREFIX }}/manifest.json

      - name: Summary
        run: echo "Uploaded $(ls dist/contexts/*.tar.gz | wc -l) template contexts + manifest to dev and prod"
