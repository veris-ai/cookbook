# Sandbox policy context: crm-analyst

This file is generated by NemoClaw. It summarises the network policy state
of the sandbox so the agent can explain why a host or integration may be
blocked and which remediation paths are available.

## Tier
- name: `balanced` (Balanced)
- description: Full dev tooling, web search, and core weather lookups. Package installs, model downloads, and inference. No messaging platform access.

## Active presets
- `brave` — Brave Search API access
  status: verified
  hosts: api.search.brave.com
- `brew` — Homebrew (Linuxbrew) package manager access (brew binary preinstalled in base image)
  status: verified
  hosts: formulae.brew.sh, ghcr.io, github.com, objects.githubusercontent.com, pkg-containers.githubusercontent.com, raw.githubusercontent.com
- `huggingface` — Hugging Face Hub, LFS, and Inference API access
  status: verified
  hosts: cdn-lfs.huggingface.co, huggingface.co, router.huggingface.co
- `npm` — npm and Yarn registry access
  status: verified
  hosts: registry.npmjs.org, registry.yarnpkg.com
- `openclaw-diagnostics-otel-local` — OpenClaw diagnostics OTLP/HTTP export to local host collector
  status: verified
  hosts: (no host endpoints declared) (1 internal host stem(s) redacted)
- `openclaw-pricing` — OpenClaw model-pricing reference fetch (LiteLLM + OpenRouter)
  status: verified
  hosts: openrouter.ai, raw.githubusercontent.com
- `posthog` [custom] — custom preset
  status: verified
  hosts: us.posthog.com
- `pypi` — Python Package Index (PyPI) access
  status: verified
  hosts: files.pythonhosted.org, pypi.org
- `slack` — Slack API, Socket Mode, and webhooks access
  status: verified
  hosts: api.slack.com, hooks.slack.com, slack.com, wss-backup.slack.com, wss-primary.slack.com
- `weather` — Read-only public weather, geocoding, and alert APIs
  status: verified
  hosts: api.open-meteo.com, api.weather.gov, geocoding-api.open-meteo.com

## Known unapplied presets
- `claude-code` — Claude Code API, telemetry, and crash-report access
- `discord` — Discord API, gateway, and CDN access
- `github` — GitHub.com and GitHub API access (git)
- `jira` — Jira and Atlassian Cloud access
- `local-inference` — Local inference access (Ollama, vLLM) via host gateway
- `nous-audio` — Nous Portal managed audio generation and transcription gateway
- `nous-browser` — Nous Portal managed browser automation gateway
- `nous-code` — Nous Portal managed sandboxed code execution gateway
- `nous-image` — Nous Portal managed image generation gateway
- `nous-web` — Nous Portal managed web search and crawl gateway
- `outlook` — Microsoft Outlook and Graph API access
- `public-reference` — Read-only structured public reference APIs
- `telegram` — Telegram Bot API access
- `wechat` — WeChat (personal) iLink API access (OpenClaw + Hermes)
- `whatsapp` — WhatsApp Web WebSocket and media access

## Approval and remediation
- inspect: `nemoclaw crm-analyst policy-list`
- add a preset: `nemoclaw crm-analyst policy-add <preset>`
- remove a preset: `nemoclaw crm-analyst policy-remove <preset>`
- documentation: docs/network-policy/customize-network-policy.mdx

## Support boundaries
- preset selection (owner: nemoclaw) — tier: Balanced
- host allowlist enforcement (owner: openshell) — policy is enforced by the OpenShell gateway
- shields toggle (owner: nemoclaw) — shields up locks down mutable config
- credential storage (owner: nemoclaw) — credentials are stored outside the policy context surface
- ad-hoc host approval (owner: external) — requests outside the applied presets require a new preset or tier change

## Failure classification
When a host or integration attempt fails, classify it as:
- `blocked-by-policy` — the host is not declared by any active preset, the request was refused with HTTP 403, or a network-block error code was returned
- `missing-approval` — the host is declared by an active preset and the request was refused with HTTP 401 (treat HTTP 403 on an active host as ambiguous between missing credentials and a finer-grained policy denial)
- `unsupported` — the capability is not offered by NemoClaw or OpenShell
- `unknown` — none of the above apply; surface the underlying error

Preset status reflects registry vs gateway agreement and is one of `verified`, `registry-only`, `gateway-only`, or `gateway-unavailable`. Treat anything other than `verified` as advisory; an agent must not assume the gateway enforces the listed hosts.

Generated at 2026-06-14T05:17:20.236Z.
